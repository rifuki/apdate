# ==============================================
# APDATE Production Configuration for VPS
# ==============================================

# Project Configuration
COMPOSE_PROJECT_NAME=apdate

# Application Environment
APP_ENV=production
CI_ENV=production

# Domain Configuration (REQUIRED - Change these!)
MAIN_DOMAIN=example.com
PHPMYADMIN_DOMAIN=phpmyadmin.example.com
BASE_URL=https://example.com

# Database Configuration (REQUIRED - Change passwords!)
DB_HOST=db
DB_NAME=apdate
DB_USER=root
DB_PASS=CHANGE_THIS_ROOT_PASSWORD_NOW
DB_USER_NON_ROOT=apdate_user
DB_USER_PASS=CHANGE_THIS_USER_PASSWORD_NOW

# Application Settings (REQUIRED - Change encryption key!)
ENCRYPTION_KEY=CHANGE_THIS_32_CHARACTER_KEY_NOW

# Email Configuration for User Verification
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-app-email@gmail.com
SMTP_PASS=your-app-specific-password
SMTP_FROM=your-app-email@gmail.com
SMTP_FROM_NAME=APDATE System

# Security Settings
SESSION_DRIVER=files
SESSION_SAVE_PATH=/tmp

# File Upload Settings
MAX_UPLOAD_SIZE=50M
UPLOAD_PATH=./upload/

# Logging Settings
LOG_THRESHOLD=1
LOG_PATH=./application/logs/

# ==============================================
# PRODUCTION SETUP CHECKLIST:
# ==============================================
#
# 1. DOMAINS (REQUIRED):
#    - Buy domain and point DNS to your VPS IP
#    - Update MAIN_DOMAIN and PHPMYADMIN_DOMAIN above
#    - Update BASE_URL to use https://yourdomain.com
#
# 2. SECURITY (CRITICAL):
#    - Change all passwords (DB_PASS, DB_USER_PASS)
#    - Generate new ENCRYPTION_KEY (32 characters)
#    - Configure email settings for notifications
#
# 3. SSL/HTTPS:
#    - Run: ./docker/prod/ssl-setup.sh
#    - Certificates will auto-renew
#
# 4. DEPLOYMENT:
#    - Copy this file to .env on your VPS
#    - Run: docker-compose -f docker-compose.prod.yml up -d
#
# 5. ACCESS:
#    - Main App: https://yourdomain.com
#    - phpMyAdmin: https://phpmyadmin.yourdomain.com
#
# 6. OPTIONAL SECURITY:
#    - Restrict phpMyAdmin access by IP in nginx config
#    - Set up fail2ban for additional protection
#    - Configure firewall (ufw)
#
# ==============================================