# ==============================================
# APDATE Docker Configuration
# ==============================================

# Project Configuration
COMPOSE_PROJECT_NAME=apdate

# Application Environment
APP_ENV=development
CI_ENV=development

# Port Configuration
APP_PORT=8080
DB_PORT=3306
PHPMYADMIN_PORT=8081

# Database Configuration for Docker
DB_HOST=db
DB_NAME=apdate
DB_USER=root
DB_PASS=root_password
DB_DRIVER=mysqli

# Optional: Non-root MySQL user (created automatically)
DB_USER_NON_ROOT=apdate_user
DB_USER_PASS=apdate_password

# Application Settings
BASE_URL=http://localhost:8080
ENCRYPTION_KEY=apdatev2

# Email Configuration (for user verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com
SMTP_FROM_NAME=APDATE System

# Security Settings
SESSION_DRIVER=files
SESSION_SAVE_PATH=/tmp

# File Upload Settings
MAX_UPLOAD_SIZE=50M
UPLOAD_PATH=./upload/

# Logging Settings
LOG_THRESHOLD=4
LOG_PATH=./application/logs/

# ==============================================
# Production Configuration Notes:
# ==============================================
# 1. Change APP_ENV=production for production
# 2. Use strong passwords for DB_PASS and DB_USER_PASS
# 3. Change ENCRYPTION_KEY to random 32-character string
# 4. Configure proper SMTP settings
# 5. Use different ports if needed (APP_PORT, DB_PORT, etc)
# ==============================================